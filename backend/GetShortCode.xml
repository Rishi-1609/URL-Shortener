This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
src/
  api/
    v1/
      health/
        health.controller.ts
        health.routes.ts
      urls/
        utils/
          generateHashedString.ts
          generateId.ts
          generateShortCode.ts
          generateShortUrl.ts
        urls.controller.ts
        urls.repository.ts
        urls.routes.ts
        urls.services.ts
        urls.validator.ts
  config/
    env.ts
  db/
    migrations/
      meta/
        _journal.json
        0000_snapshot.json
      0000_add_urls_table.sql
    schema/
      url.schema.ts
    index.ts
    test-db.ts
  middlewares/
    validate.ts
  playground/
    FindShortCode.ts
    hash.ts
    shortCode.ts
    shortUrl.ts
  app.ts
  server.ts
.env.example
drizzle.config.ts
migrateDb.ts
NewFolderStructure.xml
package.json
tsconfig.json
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="src/api/v1/health/health.controller.ts">
import { Request, Response } from "express";

export const healthController = (req : Request, res : Response) => {
    return res.status(200).json({
        status : "ok",
        timestamp : new Date().toISOString(),
        uptime : process.uptime(),
    })
}
</file>

<file path="src/api/v1/health/health.routes.ts">
import { Router } from "express";
import { healthController } from "./health.controller.js";

export const healthRouter = Router();

healthRouter.get("/", healthController);

export default healthRouter;
</file>

<file path="src/api/v1/urls/utils/generateHashedString.ts">
import { createHash } from "node:crypto";

export function generateHashedString(str : string) {
    const buffer = createHash('sha256').update(str).digest();
    const hash = BigInt('0x' + buffer.toString('hex'));
    return hash;
}
</file>

<file path="src/api/v1/urls/utils/generateId.ts">
import { ulid } from "ulidx";

export function generateId() {
    const id = ulid();
    return id;
}
</file>

<file path="src/api/v1/urls/utils/generateShortCode.ts">
import { generateHashedString } from "./generateHashedString.js";

export function generateShortCode(url : string, salt : number = 0) {
    const val = url + ":" + salt;
    const hash = generateHashedString(val);
    const base62Val = toBase62(hash).substring(0, 8);   
    return base62Val;
}

export function toBase62(hash : bigint) {
    const chars = '0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz';
    let result = '';
    let num = hash;
    while (num > 0n) {
        result = chars[Number(num % 62n)] + result;
        num /= 62n;
    }
    return result;
}
</file>

<file path="src/api/v1/urls/utils/generateShortUrl.ts">
import env from "../../../../config/env.js";

export function generateShortUrl(code : string) {
    const shortUrl = `${env.BASE_URL}/${code}`;
    return shortUrl;
}
</file>

<file path="src/api/v1/urls/urls.controller.ts">
import { Request, Response } from "express";
import { createShortUrl, getOriginalUrl } from "./urls.services.js";
import { GetShortCodeRequest } from "./urls.validator.js";

export const urlsController = {
    CreateShortUrl : async (req : Request, res : Response) => {
        try {
            const result = await createShortUrl(req.body);
            return res.status(201).json({
                success : true,
                data : result,
            })
        } catch (error) {
            console.error(error);
            return res.status(500).json({
                success : false,
                error : "Cannot create shorUrl",
            });
        }
    },

    GetOriginalUrl : async (req : Request<GetShortCodeRequest>, res : Response) => {
        try {
            const redirectUrl = await getOriginalUrl(req.params.shortCode);
            if (!redirectUrl) {
                return res.status(404).json({message : "Cannot Redirect. Invalid url"});
            }
            return res.status(301).redirect(redirectUrl);
        } catch (error) {
            console.error(error);
            return res.status(500).json({
                success : false,
                error : "Internal Server Error"
            })
        }
    }
}
</file>

<file path="src/api/v1/urls/urls.repository.ts">
import { eq } from "drizzle-orm";
import { db } from "../../../db/index.js";
import { urls } from "../../../db/schema/url.schema.js";

export async function dbInsert(id : string, originalUrl : string, shortCode : string) {
    const [result] = await db.insert(urls).values({
        id : id,
        originalUrl : originalUrl,
        shortCode : shortCode,
    }).returning();

    return result;
}

export async function dbFind(shortCode : string) {
    const result = await db.select().from(urls).where(eq(urls.shortCode, shortCode));
    return result.length !== 0;
}

export async function dbGetOriginalUrl(shortCode : string) {
    const result = await db.select().from(urls).where(eq(urls.shortCode, shortCode));
    if (result.length === 0) return null;
    const [data] = result;
    return data
}
</file>

<file path="src/api/v1/urls/urls.routes.ts">
import {Router} from "express";
import { urlsController } from "./urls.controller.js";
import { validateBody, validatePath } from "../../../middlewares/validate.js";
import { createUrlSchema, getShortCodeParamsSchema } from "./urls.validator.js";

export const urlsRouter = Router();

urlsRouter.post("/",  validateBody(createUrlSchema), urlsController.CreateShortUrl);

urlsRouter.get("/:shortCode", validatePath(getShortCodeParamsSchema), urlsController.GetOriginalUrl)
</file>

<file path="src/api/v1/urls/urls.services.ts">
import { dbFind, dbGetOriginalUrl, dbInsert } from "./urls.repository.js";
import { generateId } from "./utils/generateId.js";
import { generateShortCode } from "./utils/generateShortCode.js";
import { generateShortUrl } from "./utils/generateShortUrl.js";
import { CreateUrlRequest } from "./urls.validator.js";


export async function getOriginalUrl(shortCode : string) : Promise<string | null> {
    const originalUrl = await getOriginalUrl(shortCode);
    if (!originalUrl) return null;
    return originalUrl;
}


export async function createShortUrl(data : CreateUrlRequest) {
    
    try {
        let shortCode;
        let shortUrl;
        let id;
        let salt = 0;
        while(true) {
            id = generateId();
            shortCode = generateShortCode(data.originalUrl, salt);
            shortUrl = generateShortUrl(shortCode);
            const exists = await dbFind(shortCode);
            if (!exists)  {
                break;
            }
            ++salt;
        }
        const dbOperation = await dbInsert(id, data.originalUrl, shortCode);
        const result = {
            originalUrl : data.originalUrl,
            shortCode,
            shortUrl,
        };
        return result;
    } catch (error) {
        console.error(error);
    }
}
</file>

<file path="src/api/v1/urls/urls.validator.ts">
import { RE2 } from "re2";
import {z} from "zod";

const urlRegex = new RE2(/^(https?:\/\/)?[-a-zA-Z0-9@:%._\+~#=]{1,256}\.[a-zA-Z0-9()]{1,6}\b([-a-zA-Z0-9()@:%_\+.~#?&//=]*)$/);

export const createUrlSchema = z.object({
    originalUrl : z.string().trim().refine((val) => urlRegex.test(val), {message : "Invalid Url format"}),
})

export type CreateUrlRequest = z.infer<typeof createUrlSchema>;


export const getShortCodeParamsSchema = z.object({
    shortCode : z.string().trim().min(1),
});

export type GetShortCodeRequest = z.infer<typeof getShortCodeParamsSchema>;
</file>

<file path="src/config/env.ts">
import dotenv from "dotenv";
import {z} from "zod";
dotenv.config();

const envSchema = z.object({
    PORT : z.coerce.number().min(1).max(65535),
    NODE_ENV : z.enum(["development", "test", "production"]),
    DATABASE_URL : z.string().trim().min(1),
    BASE_URL : z.string().trim().min(1),
});

export const env = envSchema.parse(process.env);
export default env;
</file>

<file path="src/db/migrations/meta/_journal.json">
{
  "version": "7",
  "dialect": "postgresql",
  "entries": [
    {
      "idx": 0,
      "version": "7",
      "when": 1783878126432,
      "tag": "0000_add_urls_table",
      "breakpoints": true
    }
  ]
}
</file>

<file path="src/db/migrations/meta/0000_snapshot.json">
{
  "id": "71036d8d-aebe-4fab-ad75-936bb4034ad1",
  "prevId": "00000000-0000-0000-0000-000000000000",
  "version": "7",
  "dialect": "postgresql",
  "tables": {
    "public.urls": {
      "name": "urls",
      "schema": "",
      "columns": {
        "id": {
          "name": "id",
          "type": "char(26)",
          "primaryKey": true,
          "notNull": true
        },
        "original_url": {
          "name": "original_url",
          "type": "text",
          "primaryKey": false,
          "notNull": true
        },
        "short_code": {
          "name": "short_code",
          "type": "varchar(8)",
          "primaryKey": false,
          "notNull": true
        },
        "created_at": {
          "name": "created_at",
          "type": "timestamp (3) with time zone",
          "primaryKey": false,
          "notNull": true,
          "default": "now()"
        },
        "updated_at": {
          "name": "updated_at",
          "type": "timestamp (3) with time zone",
          "primaryKey": false,
          "notNull": true,
          "default": "now()"
        }
      },
      "indexes": {},
      "foreignKeys": {},
      "compositePrimaryKeys": {},
      "uniqueConstraints": {
        "urls_short_code_unique": {
          "name": "urls_short_code_unique",
          "nullsNotDistinct": false,
          "columns": [
            "short_code"
          ]
        }
      },
      "policies": {},
      "checkConstraints": {},
      "isRLSEnabled": false
    }
  },
  "enums": {},
  "schemas": {},
  "sequences": {},
  "roles": {},
  "policies": {},
  "views": {},
  "_meta": {
    "columns": {},
    "schemas": {},
    "tables": {}
  }
}
</file>

<file path="src/db/migrations/0000_add_urls_table.sql">
CREATE TABLE "urls" (
	"id" char(26) PRIMARY KEY NOT NULL,
	"original_url" text NOT NULL,
	"short_code" varchar(8) NOT NULL,
	"created_at" timestamp (3) with time zone DEFAULT now() NOT NULL,
	"updated_at" timestamp (3) with time zone DEFAULT now() NOT NULL,
	CONSTRAINT "urls_short_code_unique" UNIQUE("short_code")
);
</file>

<file path="src/db/schema/url.schema.ts">
import { char, pgTable, text, timestamp, varchar } from 'drizzle-orm/pg-core';

export const urls = pgTable("urls", {
    id : char("id", {length : 26}).primaryKey(),
    originalUrl : text("original_url").notNull(),
    shortCode : varchar("short_code", {length : 8}).notNull().unique(),
    createdAt : timestamp("created_at", {
        withTimezone : true,
        precision : 3,
    }).defaultNow().notNull(),
    updatedAt : timestamp("updated_at", {
        withTimezone : true,
        precision : 3,
    }).defaultNow().notNull(),
});
</file>

<file path="src/db/index.ts">
import { neon } from "@neondatabase/serverless";
import {drizzle} from "drizzle-orm/neon-http";
import env from "../config/env.js";

const sql = neon(env.DATABASE_URL);
export const db = drizzle({client : sql});
</file>

<file path="src/db/test-db.ts">
import { Client } from "pg";
import env from "../config/env.js";

const client = new Client({
  connectionString: env.DATABASE_URL,
});

try {
  await client.connect();
  console.log("Connected!");

  const result = await client.query(`
  SELECT table_name
  FROM information_schema.tables
  WHERE table_schema='public';
  `);
  
  console.log(result.rows);

  await client.end();
} catch (err) {
  console.error(err);
}
</file>

<file path="src/middlewares/validate.ts">
import { NextFunction, Request, Response } from "express";
import z from "zod";


const createParser = <T extends z.ZodType>(schema : T) => {
    return (input : unknown) : z.ZodSafeParseResult<z.infer<T>> => {
        return schema.safeParse(input);
    }
}


export function validateBody <T extends z.ZodType>(schema : T) {
    const parser = createParser(schema);
    return (req : Request, res : Response, next : NextFunction) => {
        const parsed = parser(req.body);
        if (!parsed.success) {
            return res.status(400).json(parsed);
        }
        req.body = parsed.data;
        next();
    };
}


export function validatePath <T extends z.ZodType>(schema : T) {
    const parser = createParser(schema);
    return (req : Request, res : Response, next: NextFunction) => {
        const parsed = parser(req.params);
        if (!parsed.success) {
            return res.status(400).json(parsed);
        }
        req.params = parsed.data as Request['params'];
        next();
    }
}
</file>

<file path="src/playground/FindShortCode.ts">
import { dbFind, dbInsert } from "../api/v1/urls/urls.repository.js";

const exist = await dbFind("ABCDEFGH");
console.log(exist);

// const result = await dbInsert("01HVY9B41VJ3E4PK5E1J5A0R0Z", "https://google.com", "ABCDEFGH");

// console.log(result);
// console.log(await dbFind("ABCDEFGH"));
</file>

<file path="src/playground/hash.ts">
import { generateHashedString } from "../api/v1/urls/utils/generateHashedString.js";

console.log(generateHashedString("hello"));
console.log(generateHashedString("hello"));
console.log(generateHashedString("world"));
console.log(generateHashedString("wolrd"));
</file>

<file path="src/playground/shortCode.ts">
import { generateShortCode } from "../api/v1/urls/utils/generateShortCode.js";

console.log(generateShortCode("https://google.com",  1));
console.log(generateShortCode("https://google.com",  1));
console.log(generateShortCode("https://google.com",  2));
console.log(generateShortCode("https://google.com",  3));
</file>

<file path="src/playground/shortUrl.ts">
import { generateShortUrl } from "../api/v1/urls/utils/generateShortUrl.js";

console.log(generateShortUrl("asdf6549"));
</file>

<file path="src/app.ts">
import express from "express";
import healthRouter from "./api/v1/health/health.routes.js";
import { urlsRouter } from "./api/v1/urls/urls.routes.js";

const app = express();

app.use(express.json());

app.get("/", (req, res) => {
    res.redirect("/health");
})

app.use("/api/v1/urls", urlsRouter);

app.use("/health", healthRouter);

export default app;
</file>

<file path="src/server.ts">
import app from "./app.js";
import { env } from "./config/env.js";

app.listen(env.PORT, ()=> {
    console.log(`Server listening at http://localhost:${env.PORT}`);
})

interface reqBody {
    originalUrl : string;
    expiresAt? : Date;
}

interface successResponse {
    success : boolean;
    data : {
        shortCode : string;
        shortUrl : string;
        expiresAt : Date;
        createdAt : Date;
    }
}

interface validationError {
    success : boolean;
    error : {
        msg : string;
        issues : {}
    }
}

interface urlDbSchema {
    id : string;
    userId : string;
    originalUrl : string;
    shortUrl : string;
    expiresAt : Date;
    createdAt : Date;
    updatedAt : Date;
    clickCount : Number;
}
</file>

<file path=".env.example">
PORT=

NODE_ENV=

BASE_URL=

DATABASE_URL=<Use a Postgresql database>
</file>

<file path="drizzle.config.ts">
import { defineConfig } from "drizzle-kit";
import {env} from "./src/config/env"

export default defineConfig({
    schema : "./src/db/schema/**/*.ts",
    out : "./src/db/migrations",
    dialect : "postgresql",
    dbCredentials : {
        url : env.DATABASE_URL,
    },
    casing : "snake_case",
})
</file>

<file path="migrateDb.ts">
import {drizzle} from "drizzle-orm/node-postgres";
import {migrate} from "drizzle-orm/node-postgres/migrator";
import {Pool, Client} from "pg";
import env from "./src/config/env";

const client = new Client({
    connectionString: env.DATABASE_URL,
});

await client.connect();

const db = drizzle(client);


try {
    console.log("Applying migrations");
    await migrate(db, {migrationsFolder : "./src/db/migrations"});
    console.log("Migrations applied successfully");
} catch (error) {
    console.error(error);
} finally {
    await client.end();
}
</file>

<file path="NewFolderStructure.xml">
This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
src/
  api/
    v1/
      health/
        health.controller.ts
        health.routes.ts
      urls/
        utils/
          generateHashedString.ts
          generateId.ts
          generateShortCode.ts
          generateShortUrl.ts
        urls.controller.ts
        urls.repository.ts
        urls.routes.ts
        urls.services.ts
        urls.validator.ts
  config/
    env.ts
  db/
    migrations/
      meta/
        _journal.json
        0000_snapshot.json
      0000_add_urls_table.sql
    schema/
      url.schema.ts
    index.ts
    test-db.ts
  middlewares/
    validate.ts
  playground/
    FindShortCode.ts
    hash.ts
    shortCode.ts
    shortUrl.ts
  app.ts
  server.ts
.env.example
drizzle.config.ts
migrateDb.ts
package.json
tsconfig.json
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="src/api/v1/health/health.controller.ts">
import { Request, Response } from "express";

export const healthController = (req : Request, res : Response) => {
    return res.status(200).json({
        status : "ok",
        timestamp : new Date().toISOString(),
        uptime : process.uptime(),
    })
}
</file>

<file path="src/api/v1/health/health.routes.ts">
import { Router } from "express";
import { healthController } from "./health.controller.js";

export const healthRouter = Router();

healthRouter.get("/", healthController);

export default healthRouter;
</file>

<file path="src/api/v1/urls/utils/generateHashedString.ts">
import { createHash } from "node:crypto";

export function generateHashedString(str : string) {
    const buffer = createHash('sha256').update(str).digest();
    const hash = BigInt('0x' + buffer.toString('hex'));
    return hash;
}
</file>

<file path="src/api/v1/urls/utils/generateId.ts">
import { ulid } from "ulidx";

export function generateId() {
    const id = ulid();
    return id;
}
</file>

<file path="src/api/v1/urls/utils/generateShortCode.ts">
import { generateHashedString } from "./generateHashedString.js";

export function generateShortCode(url : string, salt : number = 0) {
    const val = url + ":" + salt;
    const hash = generateHashedString(val);
    const base62Val = toBase62(hash).substring(0, 8);   
    return base62Val;
}

export function toBase62(hash : bigint) {
    const chars = '0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz';
    let result = '';
    let num = hash;
    while (num > 0n) {
        result = chars[Number(num % 62n)] + result;
        num /= 62n;
    }
    return result;
}
</file>

<file path="src/api/v1/urls/utils/generateShortUrl.ts">
import env from "../../../../config/env.js";

export function generateShortUrl(code : string) {
    const shortUrl = `${env.BASE_URL}/${code}`;
    return shortUrl;
}
</file>

<file path="src/api/v1/urls/urls.controller.ts">
import { Request, Response } from "express";
import { createShortUrl } from "./urls.services.js";

export const urlsController = async (req : Request, res : Response) => {
    try {
        const result = await createShortUrl(req.body);
        return res.status(201).json({
            success : true,
            data : result,
        })
    } catch (error) {
        console.error(error);
        res.status(500).json({
            success : false,
            error : "Cannot create shorUrl",
        });
    }
}
</file>

<file path="src/api/v1/urls/urls.repository.ts">
import { eq } from "drizzle-orm";
import { db } from "../../../db/index.js";
import { urls } from "../../../db/schema/url.schema.js";

export async function dbInsert(id : string, originalUrl : string, shortCode : string) {
    const [result] = await db.insert(urls).values({
        id : id,
        originalUrl : originalUrl,
        shortCode : shortCode,
    }).returning();

    return result;
}

export async function dbFind(shortCode : string) {
    const result = await db.select().from(urls).where(eq(urls.shortCode, shortCode));
    return result.length !== 0;
}
</file>

<file path="src/api/v1/urls/urls.routes.ts">
import {Router} from "express";
import { urlsController } from "./urls.controller.js";
import { validate } from "../../../middlewares/validate.js";
import { createUrlSchema } from "./urls.validator.js";

export const urlsRouter = Router();

urlsRouter.post("/",  validate(createUrlSchema), urlsController);
</file>

<file path="src/api/v1/urls/urls.services.ts">
import { dbFind, dbInsert } from "./urls.repository.js";
import { generateId } from "./utils/generateId.js";
import { generateShortCode } from "./utils/generateShortCode.js";
import { generateShortUrl } from "./utils/generateShortUrl.js";
import { CreateUrlRequest } from "./urls.validator.js";


export async function createShortUrl(data : CreateUrlRequest) {
    
    try {
        let shortCode;
        let shortUrl;
        let id;
        let salt = 0;
        while(true) {
            id = generateId();
            shortCode = generateShortCode(data.originalUrl, salt);
            shortUrl = generateShortUrl(shortCode);
            const exists = await dbFind(shortCode);
            if (!exists)  {
                break;
            }
            ++salt;
        }
        const dbOperation = await dbInsert(id, data.originalUrl, shortCode);
        const result = {
            originalUrl : data.originalUrl,
            shortCode,
            shortUrl,
        };
        return result;
    } catch (error) {
        console.error(error);
    }
}
</file>

<file path="src/api/v1/urls/urls.validator.ts">
import { RE2 } from "re2";
import {z} from "zod";

const urlRegex = new RE2(/^(https?:\/\/)?[-a-zA-Z0-9@:%._\+~#=]{1,256}\.[a-zA-Z0-9()]{1,6}\b([-a-zA-Z0-9()@:%_\+.~#?&//=]*)$/);

export const createUrlSchema = z.object({
    originalUrl : z.string().trim().refine((val) => urlRegex.test(val), {message : "Invalid Url format"}),
})

export type CreateUrlRequest = z.infer<typeof createUrlSchema>;
</file>

<file path="src/config/env.ts">
import dotenv from "dotenv";
import {z} from "zod";
dotenv.config();

const envSchema = z.object({
    PORT : z.coerce.number().min(1).max(65535),
    NODE_ENV : z.enum(["development", "test", "production"]),
    DATABASE_URL : z.string().trim().min(1),
    BASE_URL : z.string().trim().min(1),
});

export const env = envSchema.parse(process.env);
export default env;
</file>

<file path="src/db/migrations/meta/_journal.json">
{
  "version": "7",
  "dialect": "postgresql",
  "entries": [
    {
      "idx": 0,
      "version": "7",
      "when": 1783878126432,
      "tag": "0000_add_urls_table",
      "breakpoints": true
    }
  ]
}
</file>

<file path="src/db/migrations/meta/0000_snapshot.json">
{
  "id": "71036d8d-aebe-4fab-ad75-936bb4034ad1",
  "prevId": "00000000-0000-0000-0000-000000000000",
  "version": "7",
  "dialect": "postgresql",
  "tables": {
    "public.urls": {
      "name": "urls",
      "schema": "",
      "columns": {
        "id": {
          "name": "id",
          "type": "char(26)",
          "primaryKey": true,
          "notNull": true
        },
        "original_url": {
          "name": "original_url",
          "type": "text",
          "primaryKey": false,
          "notNull": true
        },
        "short_code": {
          "name": "short_code",
          "type": "varchar(8)",
          "primaryKey": false,
          "notNull": true
        },
        "created_at": {
          "name": "created_at",
          "type": "timestamp (3) with time zone",
          "primaryKey": false,
          "notNull": true,
          "default": "now()"
        },
        "updated_at": {
          "name": "updated_at",
          "type": "timestamp (3) with time zone",
          "primaryKey": false,
          "notNull": true,
          "default": "now()"
        }
      },
      "indexes": {},
      "foreignKeys": {},
      "compositePrimaryKeys": {},
      "uniqueConstraints": {
        "urls_short_code_unique": {
          "name": "urls_short_code_unique",
          "nullsNotDistinct": false,
          "columns": [
            "short_code"
          ]
        }
      },
      "policies": {},
      "checkConstraints": {},
      "isRLSEnabled": false
    }
  },
  "enums": {},
  "schemas": {},
  "sequences": {},
  "roles": {},
  "policies": {},
  "views": {},
  "_meta": {
    "columns": {},
    "schemas": {},
    "tables": {}
  }
}
</file>

<file path="src/db/migrations/0000_add_urls_table.sql">
CREATE TABLE "urls" (
	"id" char(26) PRIMARY KEY NOT NULL,
	"original_url" text NOT NULL,
	"short_code" varchar(8) NOT NULL,
	"created_at" timestamp (3) with time zone DEFAULT now() NOT NULL,
	"updated_at" timestamp (3) with time zone DEFAULT now() NOT NULL,
	CONSTRAINT "urls_short_code_unique" UNIQUE("short_code")
);
</file>

<file path="src/db/schema/url.schema.ts">
import { char, pgTable, text, timestamp, varchar } from 'drizzle-orm/pg-core';

export const urls = pgTable("urls", {
    id : char("id", {length : 26}).primaryKey(),
    originalUrl : text("original_url").notNull(),
    shortCode : varchar("short_code", {length : 8}).notNull().unique(),
    createdAt : timestamp("created_at", {
        withTimezone : true,
        precision : 3,
    }).defaultNow().notNull(),
    updatedAt : timestamp("updated_at", {
        withTimezone : true,
        precision : 3,
    }).defaultNow().notNull(),
});
</file>

<file path="src/db/index.ts">
import { neon } from "@neondatabase/serverless";
import {drizzle} from "drizzle-orm/neon-http";
import env from "../config/env.js";

const sql = neon(env.DATABASE_URL);
export const db = drizzle({client : sql});
</file>

<file path="src/db/test-db.ts">
import { Client } from "pg";
import env from "../config/env.js";

const client = new Client({
  connectionString: env.DATABASE_URL,
});

try {
  await client.connect();
  console.log("Connected!");

  const result = await client.query(`
  SELECT table_name
  FROM information_schema.tables
  WHERE table_schema='public';
  `);
  
  console.log(result.rows);

  await client.end();
} catch (err) {
  console.error(err);
}
</file>

<file path="src/middlewares/validate.ts">
import { NextFunction, Request, Response } from "express";
import z from "zod";


const createParser = <T extends z.ZodType>(schema : T) => {
    return (input : unknown) : z.ZodSafeParseResult<z.infer<T>> => {
        return schema.safeParse(input);
    }
}


export function validate <T extends z.ZodType>(schema : T) {
    const parser = createParser(schema);
    return (req : Request, res : Response, next : NextFunction) => {
        const parsed = parser(req.body);
        if (!parsed.success) {
            return res.status(400).json(parsed);
        }
        req.body = parsed.data;
        next();
    };
}
</file>

<file path="src/playground/FindShortCode.ts">
import { dbFind, dbInsert } from "../api/v1/urls/urls.repository.js";

const exist = await dbFind("ABCDEFGH");
console.log(exist);

// const result = await dbInsert("01HVY9B41VJ3E4PK5E1J5A0R0Z", "https://google.com", "ABCDEFGH");

// console.log(result);
// console.log(await dbFind("ABCDEFGH"));
</file>

<file path="src/playground/hash.ts">
import { generateHashedString } from "../api/v1/urls/utils/generateHashedString.js";

console.log(generateHashedString("hello"));
console.log(generateHashedString("hello"));
console.log(generateHashedString("world"));
console.log(generateHashedString("wolrd"));
</file>

<file path="src/playground/shortCode.ts">
import { generateShortCode } from "../api/v1/urls/utils/generateShortCode.js";

console.log(generateShortCode("https://google.com",  1));
console.log(generateShortCode("https://google.com",  1));
console.log(generateShortCode("https://google.com",  2));
console.log(generateShortCode("https://google.com",  3));
</file>

<file path="src/playground/shortUrl.ts">
import { generateShortUrl } from "../api/v1/urls/utils/generateShortUrl.js";

console.log(generateShortUrl("asdf6549"));
</file>

<file path="src/app.ts">
import express from "express";
import healthRouter from "./api/v1/health/health.routes.js";
import { urlsRouter } from "./api/v1/urls/urls.routes.js";

const app = express();

app.use(express.json());

app.get("/", (req, res) => {
    res.redirect("/health");
})

app.use("/api/v1/urls", urlsRouter);

app.use("/health", healthRouter);

export default app;
</file>

<file path="src/server.ts">
import app from "./app.js";
import { env } from "./config/env.js";

app.listen(env.PORT, ()=> {
    console.log(`Server listening at http://localhost:${env.PORT}`);
})

interface reqBody {
    originalUrl : string;
    expiresAt? : Date;
}

interface successResponse {
    success : boolean;
    data : {
        shortCode : string;
        shortUrl : string;
        expiresAt : Date;
        createdAt : Date;
    }
}

interface validationError {
    success : boolean;
    error : {
        msg : string;
        issues : {}
    }
}

interface urlDbSchema {
    id : string;
    userId : string;
    originalUrl : string;
    shortUrl : string;
    expiresAt : Date;
    createdAt : Date;
    updatedAt : Date;
    clickCount : Number;
}
</file>

<file path=".env.example">
PORT=

NODE_ENV=

BASE_URL=

DATABASE_URL=<Use a Postgresql database>
</file>

<file path="drizzle.config.ts">
import { defineConfig } from "drizzle-kit";
import {env} from "./src/config/env"

export default defineConfig({
    schema : "./src/db/schema/**/*.ts",
    out : "./src/db/migrations",
    dialect : "postgresql",
    dbCredentials : {
        url : env.DATABASE_URL,
    },
    casing : "snake_case",
})
</file>

<file path="migrateDb.ts">
import {drizzle} from "drizzle-orm/node-postgres";
import {migrate} from "drizzle-orm/node-postgres/migrator";
import {Pool, Client} from "pg";
import env from "./src/config/env";

const client = new Client({
    connectionString: env.DATABASE_URL,
});

await client.connect();

const db = drizzle(client);


try {
    console.log("Applying migrations");
    await migrate(db, {migrationsFolder : "./src/db/migrations"});
    console.log("Migrations applied successfully");
} catch (error) {
    console.error(error);
} finally {
    await client.end();
}
</file>

<file path="package.json">
{
  "name": "url-shortener-backend",
  "version": "1.0.0",
  "description": "Backend Architecture for the Url Shortener App",
  "homepage": "https://github.com/Rishi-1609/URL-Shortener#readme",
  "bugs": {
    "url": "https://github.com/Rishi-1609/URL-Shortener/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Rishi-1609/URL-Shortener.git"
  },
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "drizzle-orm": "^0.45.2",
    "express": "^5.2.1",
    "pg": "^8.22.0",
    "re2": "^1.26.0",
    "ulidx": "^2.4.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "@types/express": "^5.0.6",
    "@types/node": "^26.1.1",
    "@types/pg": "^8.20.0",
    "drizzle-kit": "^0.31.10",
    "repomix": "^1.16.0",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3"
  }
}
</file>

<file path="tsconfig.json">
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "nodenext",
    "moduleResolution": "nodenext",

    "rootDir": "./src",
    "outDir": "./dist",

    "strict": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,

    "types": ["node"]
  },
  "sourceMap": true,

  "include": ["src"],
  "exclude": ["node_modules", "dist"]
}
</file>

</files>
</file>

<file path="package.json">
{
  "name": "url-shortener-backend",
  "version": "1.0.0",
  "description": "Backend Architecture for the Url Shortener App",
  "homepage": "https://github.com/Rishi-1609/URL-Shortener#readme",
  "bugs": {
    "url": "https://github.com/Rishi-1609/URL-Shortener/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Rishi-1609/URL-Shortener.git"
  },
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "drizzle-orm": "^0.45.2",
    "express": "^5.2.1",
    "pg": "^8.22.0",
    "re2": "^1.26.0",
    "ulidx": "^2.4.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "@types/express": "^5.0.6",
    "@types/node": "^26.1.1",
    "@types/pg": "^8.20.0",
    "drizzle-kit": "^0.31.10",
    "repomix": "^1.16.0",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3"
  }
}
</file>

<file path="tsconfig.json">
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "nodenext",
    "moduleResolution": "nodenext",

    "rootDir": "./src",
    "outDir": "./dist",

    "strict": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,

    "types": ["node"]
  },
  "sourceMap": true,

  "include": ["src"],
  "exclude": ["node_modules", "dist"]
}
</file>

</files>
